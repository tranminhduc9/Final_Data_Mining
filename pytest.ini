[pytest]
markers =
    fast: Fast unit-style tests for data pipeline logic.
    scrape: Scraper-oriented tests (mocked/integration-like) for data collection flows.
    integration: Live tests that require external services like Neo4j and are opt-in only.
